<?xml version="1.0" encoding="utf-8"?>
<!-- Cell spacing is applied by the RecyclerView ItemDecoration, not a layout_margin here. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="140dp"
    android:background="#22FFFFFF">

    <ImageView
        android:id="@+id/thumb"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:contentDescription="@string/cd_thumbnail" />

    <TextView
        android:id="@+id/name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:background="#99000000"
        android:paddingHorizontal="4dp"
        android:paddingVertical="2dp"
        android:singleLine="true"
        android:textColor="#FFFFFF"
        android:textSize="10sp" />

    <!-- Media badge: favorite when starred, else a type hint (photo / video / panorama / burst). Icon set
         in the adapter. A translucent plate keeps the white glyph legible over any thumbnail. -->
    <ImageView
        android:id="@+id/star"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_gravity="top|start"
        android:layout_marginStart="6dp"
        android:layout_marginTop="9dp"
        android:background="@drawable/badge_plate"
        android:padding="6dp"
        android:scaleType="fitCenter"
        android:contentDescription="@string/cd_media_badge" />

    <TextView
        android:id="@+id/backupState"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:layout_marginBottom="20dp"
        android:padding="4dp"
        android:background="#DD000000"
        android:textColor="#FFFFFF"
        android:textSize="11sp"
        android:maxLines="3"
        android:visibility="gone" />

    <CheckBox
        android:id="@+id/check"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|end"
        android:clickable="false"
        android:focusable="false" />
</FrameLayout>
