<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- gzipped "Save logs" files staged for the share sheet -->
    <external-cache-path name="shared_logs" path="shared_logs/" />
    <!-- Typed diagnostic exports are staged only for the user-initiated share sheet. -->
    <external-cache-path name="shared_diagnostics" path="shared_diagnostics/" />
</paths>
