# Strict DJI local-time implementation checkpoint

2026-09-17; user-directed G2 scope, ADR0011. Strict full-basename/calendar parser, explicit source priority and camera-local time without phone-derived offset. Supported uppercase `_D` names/extensions documented in capture-day design. New schema4 table captures asset-linked time evidence separately from frozen paths; same-day fallback metadata can improve, different-day evidence records reservationDayConflict without relocation. No dependency, Gradle, protocol, media, branding or release change.

## Verification

- First full command failed76s:346/347 tests passed; old adapter assertion required no filename time, superseded by explicit user requirement. All12 new filename tests passed. Updated old assertion to require local-only DJI evidence and preserve no-invented-companion/offset/instant checks; no production workaround.
- Focused parser/adapter21 tests + test-APK build PASS30s. Covers exact/invalid/leap/malformed/non-DJI/unsupported names, timezone unknown, differing phone zone, midnight, delayed sync, remote priority and stronger trusted metadata.
- Final full347 unit tests PASS; release compilation PASS. Final command40s exit1 only lint baseline5 errors/78 warnings/2 hints (CoarseFineLocation and UseAppTint), no new lint findings or suppression. Debug APK build passed in first command before old-test failure; final source-normalized comparison verifies tested artifacts correspond to committed source. Exact APK hashes and12-file scope in VALIDATION.json.
- Isolated emulator explicit `-s emulator-5580`, install-r both APKs, `am instrument -w -e ledgerPhase suite ...LauncherSecurityInstrumentation`: PASS. Includes actual3→4 migration fixture retaining rows, new evidence persistence after reopen, same-day metadata upgrade, different-day conflict and unchanged reserved paths; earlier1/2 migration chains now end at4. Existing suite's text label still says1/2-to-3; assertions now check4. Prior ledger/candidate/provider/privacy/corruption tests PASS; synthetic indexed-size lag observed=true in this run, no user-media implication. Separate normal/hostile B1 runner PASS.
- Commands use existing LF checkout/JDK/SDK, `--no-build-cache --console=plain --no-daemon`. Original comprehensive run: assembleDebug, assembleDebugAndroidTest, compileReleaseKotlin, testDebugUnitTest, lintDebug. Focused run: testDebugUnitTest --tests '*DjiFilenameTimeTest' --tests '*LedgerEnumeratorTest', assembleDebugAndroidTest. Final: testDebugUnitTest, compileReleaseKotlin, lintDebug. No repeated full run after checkpoint PASS.

## Target read-only reevaluation / remaining physical step

Verified APKs installed with `adb -d install -r` Success, no data clear. Debug SHA2563D24BDD4CC34FD16BC2CB67A455EF7B515A42347912EAEF5482018E644F6B052; test D7D9F2EA5C0E4F60CA44D75B2A6D0BF6A3E6A59E2697260BE9F6618D5D9FB800. Read-only `hardwareAudit=read-only timestampEvidence=read-only` returned [exact sanitized result](target-readonly-reevaluation.txt): third asset DJI_20260917184037_0003_D.MP4 parses2026-09-17T18:40:37, day2026-09-17, offset null. Candidate evaluated by production parser against its stored remote basename, not user clock input. No absolute Instant inferred. Approximate external plausibility is user-confirmed only, not exact-minute truth.

This audit does not initialize Room or write the ledger. Old stored third recording still reports sync timestamp2026-09-17T16:46:04.285117Z until normal camera re-enumeration; do not call candidate projection a persisted update. Desired destination day already matches2026-09-17, existing relative leaf/path remains stable. First protected asset's filename day2026-09-16 differs from old reserved2026-09-17; keep old path and persist separate conflict at ingestion. No silent move or duplicate destination.

Protected stat before/after install/read-only audit:38447651 and3071380142 unchanged; no content reads/hashes/download/delete/rename. Ordinary MainActivity launch COLD/ok287ms. Combined target command9.52s exit0. Next required user step: one normal fresh Rescan/connection, confirm3 media, select/download nothing. Then audit schema4 and actual persisted time evidence. Overall G2 BLOCKED; absolute clock/timezone and broader all-original/sidecar claims not invented. Cost-aware policy accepted in AGENTS; related findings batched here.
