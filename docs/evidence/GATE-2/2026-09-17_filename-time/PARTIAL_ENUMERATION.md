# Partial enumeration and additional weak-identity candidate

2026-09-17, user sees only the approximately38MB video after requested rescan; previously known3. No media selected/transferred/deleted/modified. Observation only, not evidence of source deletion. Current app-process allowlisted diagnostics before audit: playback mode held; SD_RETRY_COUNT=0; PARSED_COUNT=1; MANIFEST_COUNT=1; playback already held. Raw logcat not persisted. These establish one decoded record, not trustworthy full enumeration or a specific session/power cause.

Read-only audits (same hardwareAudit and timestampEvidence commands as REPORT.md), exit0, combined1.48s: [ledger](partial-enumeration-audit.txt), [time projection](partial-enumeration-times.txt). Audit intentionally replaces the process; no live retry/transfer executed.

- Actual schema4, six snapshots, **four** assets/recordings/replicas. Latest snapshot6964d01a5f4333d7c55553f02c365eebcabf71f1eb9dd968f7ab1bd7e1121600: one member, INCOMPLETE, all_coverage_proven=false. Earlier three-member snapshot retained.
- All three historical assets remain; old local references and paths retained. No removed/source-empty/complete inference.
- Additional row00cbbe2339bbbb15ad499ee138fe447dcb690afdfafaab5b83240217995e3374 has same displayed remote basename as the38MB file but size=null. It is the only latest-snapshot member. NEEDS_REVALIDATION, AMBIGUOUS, one candidate, no selected local locator, REVALIDATE_IDENTITY; not proven a distinct camera original or proven same byte identity.
- Prior38MB row retains LOCAL_PRESENT_UNVERIFIED and local reference, but audit's effective cross-owner presence is now AMBIGUOUS and policy REVALIDATE_IDENTITY. Large-file row remains VERIFY_EXISTING. Third historical row still has DISCOVERED/ABSENT and per-row DOWNLOAD policy, **not** membership in the current snapshot or a queued transfer. No download started.
- New weak row has persisted DJI_FILENAME local time2026-09-16T14:33:29, null zone and2026-09-16 reservation; this is not a successful upgrade of the original38MB row. Third asset still has historical SYNC_FALLBACK; desired third-asset persistence test remains NOT_TESTED.
- Protected stat sizes38447651 and3071380142 unchanged. No content read/hash, rename, delete, merge or manual database repair.

Source review: identity includes storage/path/size/remoteTime/mediaType/handle/strongVersion; lookup/upsert uses that fingerprint. Missing size alone is sufficient to change it if other fields stay equal; other metadata differences were not exposed by this projection. This supports a metadata-degradation identity-fragmentation finding, not proof that size is the only changed input or that a camera original changed. Preserve evidence and all rows; do not deduplicate on filename or reuse a verified identity without stronger proof. Current safety behavior requires revalidation instead of blind adoption/transfer, but duplicate-free hardware acceptance is reopened for degraded enumeration. Normal earlier full-enumeration PASS remains historical evidence, not this condition's PASS.

Least-invasive recovery: built-in SD retry already returned0. Reopened normal MainActivity (COLD/ok219ms) after audit. Next step is a single ordinary fresh Rescan with camera awake, no power-cycle/settings/Back to Live View or media action. Need physical UI/camera confirmation; no safe external read-only enumeration hook exists in current flow. Capture restored metadata before deciding identity-reconciliation correction; do not speculative-fix protocol or erase variants. G2 BLOCKED for partial inventory and identity ambiguity; root cause unconfirmed. No further production change in this checkpoint.
