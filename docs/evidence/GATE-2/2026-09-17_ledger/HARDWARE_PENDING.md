# Target hardware validation still required

Status: NOT_TESTED; PAUSED_BY_USER. No physical ADB enumeration, camera interaction, media hash/copy/transfer/delete or settings change occurred in G2 software work.

When the user explicitly returns, connect S25 Ultra over USB with debugging approval and power on Pocket4P. First record APK commit/path/SHA256 and current phone/camera clock displays before connection because existing upstream connect may synchronize camera time. Preserve completed user files and camera originals. Install the verified debug artifact without clearing app data only after normal device/package checks.

1. Connect through ordinary UI; verify private ledger counts/classifications/uncertainty and INCOMPLETE inventory truth using sanitized technical projections, never raw credentials/database export/logcat.
2. Repeat enumeration and controlled app restart; compare asset/group counts and persisted opaque path identities. Snapshot IDs may differ; repeated assets must not multiply.
3. Add only a new non-critical recording if needed. Verify NEW candidate and deterministic missing/revalidation plan. No automatic transfer is implemented in G2 and no same-name/size file may be adopted as verified.
4. Inspect available timestamp/type evidence and expected capture day; Pocket filename trust/offset and companion relationships remain unproved. Explicit fallback must remain visible until independently established. Do not fabricate expected real capture dates from the phone clock.
5. Verify optional-source types/unknown preservation and no invented sidecar grouping. Capture-day folders are persisted reservations at G2; physical storage allocation and integrity belong to later gates.
6. Validate secure credential persistence across restart and normal connection without recording a value. Actual OEM D2D/backup and screen-off key behavior need dedicated safe hardware evidence before broad claims.

Hardware is the anticipated eventual stop boundary; software result is finalized only after all regression/security/evidence checks complete. Gate2 PASS requires its mandatory hardware evidence. Existing baseline AUTO_RECONNECT/AUTO_RESUME failures, sleep uncertainty and foreground-drop root cause remain separate and unchanged.
