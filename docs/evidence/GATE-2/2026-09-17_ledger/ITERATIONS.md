# Original failures / corrections

- First Room/KSP compile succeeded (70s), schema1 exported. Second build and30 pure ledger tests succeeded (77s), debug/test APKs and release Kotlin compiled.
- First ledger emulator harness returned `FAIL: synthetic ledger precondition` before DB assertions. Read-only emulator properties confirmed official API36 model `Android SDK built for x86_64`; harness compared lowercase `sdk` case-sensitively. Corrected emulator-only guard to ranchu hardware plus case-insensitive SDK model. No test assertion relaxed and no physical device accessed.
- Third build:39 ledger unit tests/debug APK/test APK passed; lint failed with exactly five baseline errors (CoarseFineLocation + four UseAppTint),78 warnings/two hints; no suppression. Corrected-guard emulator suite passed initial real Room, FK, rollback and migration assertions. Expanded further tests cover retained partial/group/unknown through migration, concurrent repeated request, Activity recreation and corruption preservation.
- Expanded suite first failed at sqlite-full-rollback: max_page_count had been set through an unpinned query connection, so it did not constrain the writer connection used by Room. Moved the pragma and insertion into the same transaction/connection; retain the rollback assertion and rerun. Credential migration, including synthetic interruption, cross-camera authentication, missing-key/re-entry and ciphertext tampering assertions, ran before this failure and passed.
- Durability hardening compile attempt failed because O_DIRECTORY is not exposed by the Android SDK OsConstants surface. Corrected to the supported read-only directory descriptor (private validated app path), retaining file and directory fsync before retiring plaintext; rerun required.
- Final source review found that coordinator snapshots recorded ingestion start rather than actual initial protocol enumeration start. Persist the captured enumeration start through pagination and capture observation end before queuing; preserve it across cumulative page snapshots. No media/transfer behavior changed. Recompile/retest before publication of this correction.
- Corrected durability implementation passes all48 focused unit tests, debug/test packaging and the full emulator suite including credential stages. Strict SQLiteFullException assertion was added and passed; all original failed attempts remain documented. Initial authoritative LF full regression passes313 tests. Final enumeration-timestamp correction is a separate additive commit with repeat regression, no history rewrite.
- Final database-level cases verify two capture days, parent audio grouping, retained partial paths after delayed sync/travel-zone change and same-metadata weak identity reuse. Fresh emulator session initially had no target package; instrumentation failed before tests. Reinstalled the exact verified LF APK, granted only emulator Bluetooth test permissions, then suite/persist/force-stop/restore/B1 all passed. Emulator was stopped afterward.
